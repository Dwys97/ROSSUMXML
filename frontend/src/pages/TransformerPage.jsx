// In frontend/src/pages/TransformerPage.jsx

import React from 'react';

export default function TransformerPage() {
  // We will build the transformer UI here in the next step
  return (
    <div>
      <h1>XML Transformer Tool</h1>
      <p>This is where the transformer component will go.</p>
    </div>
  );
}