# Lightweight ML Service Requirements
# Optimized for 31GB codespace constraints
# Uses Surya OCR + Existing LayoutLMv3 (no LLMs)

# Core dependencies
flask>=3.0.0
flask-cors>=4.0.0
pillow>=10.1.0
numpy>=1.26.0

# Surya OCR (Layout-Aware) - lightweight
surya-ocr>=0.4.14

# Existing dependencies (already installed)
paddleocr>=2.7.0

# PDF processing
PyMuPDF>=1.23.8

# Image preprocessing
opencv-python-headless>=4.8.1.78
