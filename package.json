{
  "name": "xml-transformer-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:backend": "sam local start-api --template-file backend/template.yml --port 3000",
    "start:frontend": "npm run --prefix frontend dev",
    "dev": "concurrently \"npm:start:*\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.11"
  }
}
