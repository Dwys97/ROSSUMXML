{
  "name": "rossumxml",
  "version": "1.0.0",
"scripts": {
  "start:all": "concurrently --names \"DOCKER,VITE\" -c \"blue,cyan\" \"docker-compose up --build\" \"cd frontend && npm run dev\""
},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}