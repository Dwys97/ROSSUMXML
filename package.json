{
  "name": "xml-transformer-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start:backend": "cd backend && /opt/conda/bin/sam local start-api --port 3000",
    "start:frontend": "npm run --prefix frontend dev",
    "dev": "concurrently \"npm:start:*\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }

}
