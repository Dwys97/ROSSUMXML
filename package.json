{
  "name": "xml-transformer-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend",
    "start:backend": "sam local start-api --template-file backend/template.yml --port 3000 --debug",
    "start:frontend": "python3 -m http.server 8080 --directory frontend",
    "dev": "concurrently \"npm:start:*\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}